animation := import("animation")
audio := import("audio")
physics := import("physics")
hazard := import("hazard")

deathState := {
    enter: func(state) {
        audio.play("death")
        animation.set("death")
        hazard.disable()
    },

    update: func(state) {
        physics.stop_x()
    },

    exit: func(state) {}
}
